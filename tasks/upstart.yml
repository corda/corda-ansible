---
- name: Install upstart startup file
  become: yes
  template:
    src: "{{ item }}.init.j2"
    dest: /etc/init/{{ item }}.conf
    owner: root
    group: root
    mode: 0644
  loop:
    - corda
  notify:
    - restart corda

[Unit]
Description={{ name }}
Requires=network.target

[Service]
Type=Simple
User={{ user }}
WorkingDirectory={{ dir_location }}
ExecStart=/usr/bin/java -Xmx2048m -jar {{ dir_location }}/{{ name }}.jar
Restart=on-failure

[Install]
WantedBy=multi-user.target
